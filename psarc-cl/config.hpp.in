#pragma once

#define PSARC_CL_COMPILER  \
  "${CMAKE_C_COMPILER_ID}" \
  " "                      \
  "${CMAKE_C_COMPILER_VERSION}"
#define PSARC_CL_OS      \
  "${CMAKE_SYSTEM_NAME}" \
  " "                    \
  "${CMAKE_SYSTEM_VERSION}"
#define PSARC_CL_VERSION_DATE "${GIT_COMMIT_DATE}"
#define PSARC_CL_BRANCH_NAME "${GIT_BRANCH_NAME}"
#define PSARC_CL_VERSION_HASH "${GIT_COMMIT_HASH}"
